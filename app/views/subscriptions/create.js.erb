showFlash("<%= j render('shared/flash') %>");

var subscript = document.querySelector('.subscriptions').querySelector('a');

subscript.text = "Unfollow";
subscript.href = "/subscriptions/" + "<%= @subscription.id %>";
subscript.dataset.method = "delete"
