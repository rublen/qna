showFlash("<%= j render 'shared/flash' %>");

var answerErrors = document.querySelector('.answer-errors');
var answerNode = document.getElementById('answer-' + "<%= @answer.id %>");

answerErrors.innerHTML = "<%= j render 'shared/errors', resource: @answer %>";

if (answerErrors.innerHTML === '') {
  answerNode.innerHTML = "<%= j render @answer %>".trim()
}
