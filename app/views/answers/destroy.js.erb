showFlash("<%= j render 'shared/flash' %>");

var answers = document.querySelector('.answers');
var answer = answers.querySelector("#answer-" + <%= @answer.id %>);

answer.parentNode.removeChild(answer);
